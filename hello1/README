使用的web-sys库要在Cargo.toml中引用
注意要引用jscast否则不能做jsvalue到jsfunction的转换

编译:
	wasm-pack build --target bundler

使用到的wasm相关功能:
	- document.getElementById
	- create element
	- append element to dom
	- add event listener to element
	- export rust struct (and methods) to js
